{
  "$defs": {
    "CertificateRequest": {
      "properties": {
        "certificate_signing_request": {
          "description": "PEM encoded certificate signing request.",
          "title": "Certificate Signing Request",
          "type": "string"
        },
        "ca": {
          "description": "Whether the certificate is a CA certificate.",
          "title": "Ca",
          "type": "boolean"
        }
      },
      "required": [
        "certificate_signing_request",
        "ca"
      ],
      "title": "CertificateRequest",
      "type": "object"
    },
    "RequirerApplicationDataModel": {
      "properties": {
        "certificate_signing_requests": {
          "contentMediaType": "application/json",
          "contentSchema": {
            "items": {
              "$ref": "#/$defs/CertificateRequest"
            },
            "type": "array"
          },
          "description": "List of Certificate Signing Requests.",
          "title": "Certificate Signing Requests",
          "type": "string"
        }
      },
      "required": [
        "certificate_signing_requests"
      ],
      "title": "RequirerApplicationDataModel",
      "type": "object"
    },
    "RequirerUnitDataModel": {
      "properties": {
        "certificate_signing_requests": {
          "contentMediaType": "application/json",
          "contentSchema": {
            "items": {
              "$ref": "#/$defs/CertificateRequest"
            },
            "type": "array"
          },
          "description": "List of Certificate Signing Requests.",
          "title": "Certificate Signing Requests",
          "type": "string"
        }
      },
      "required": [
        "certificate_signing_requests"
      ],
      "title": "RequirerUnitDataModel",
      "type": "object"
    }
  },
  "description": "Requirer schema for TLS Certificates.",
  "properties": {
    "unit": {
      "$ref": "#/$defs/RequirerUnitDataModel"
    },
    "app": {
      "$ref": "#/$defs/RequirerApplicationDataModel"
    }
  },
  "required": [
    "unit",
    "app"
  ],
  "title": "RequirerSchema",
  "type": "object"
}