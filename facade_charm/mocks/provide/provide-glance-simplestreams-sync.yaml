
# databag template for glance-simplestreams-sync provider:
# this data will be put in the application/unit databag of any relation bound on provide-glance-simplestreams-sync

# must be str:str
app_data: 
    # foo: bar
    
# must be str:str
units_data:
    # baz: qux
