
# databag template for prometheus_scrape provider:
# this data will be put in the application/unit databag of any relation bound on provide-prometheus_scrape

# must be str:str
app_data: 
    # foo: bar
    
# must be str:str
units_data:
    # baz: qux
