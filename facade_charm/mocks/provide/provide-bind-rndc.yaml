
# databag template for bind-rndc provider:
# this data will be put in the application/unit databag of any relation bound on provide-bind-rndc

# must be str:str
app_data: 
    # foo: bar
    
# must be str:str
units_data:
    # baz: qux
