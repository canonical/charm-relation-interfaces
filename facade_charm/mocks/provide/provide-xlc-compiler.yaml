
# databag template for xlc-compiler provider:
# this data will be put in the application/unit databag of any relation bound on provide-xlc-compiler

# must be str:str
app_data: 
    # foo: bar
    
# must be str:str
units_data:
    # baz: qux
