
# databag template for sentry-metrics provider:
# this data will be put in the application/unit databag of any relation bound on provide-sentry-metrics

# must be str:str
app_data: 
    # foo: bar
    
# must be str:str
units_data:
    # baz: qux
