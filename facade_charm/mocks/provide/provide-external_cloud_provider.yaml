
# databag template for external_cloud_provider provider:
# this data will be put in the application/unit databag of any relation bound on provide-external_cloud_provider

# must be str:str
app_data: 
    # foo: bar
    
# must be str:str
units_data:
    # baz: qux
