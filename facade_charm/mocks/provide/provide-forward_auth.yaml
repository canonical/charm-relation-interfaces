
# databag template for forward_auth provider:
# this data will be put in the application/unit databag of any relation bound on provide-forward_auth

# must be str:str
app_data: 
    # foo: bar
    
# must be str:str
units_data:
    # baz: qux
