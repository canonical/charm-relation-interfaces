
# databag template for prometheus_remote_write provider:
# this data will be put in the application/unit databag of any relation bound on provide-prometheus_remote_write

# must be str:str
app_data: 
    # foo: bar
    
# must be str:str
units_data:
    # baz: qux
