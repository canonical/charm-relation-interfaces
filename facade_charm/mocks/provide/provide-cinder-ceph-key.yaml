
# databag template for cinder-ceph-key provider:
# this data will be put in the application/unit databag of any relation bound on provide-cinder-ceph-key

# must be str:str
app_data: 
    # foo: bar
    
# must be str:str
units_data:
    # baz: qux
