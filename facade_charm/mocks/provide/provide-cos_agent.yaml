
# databag template for cos_agent provider:
# this data will be put in the application/unit databag of any relation bound on provide-cos_agent

# must be str:str
app_data: 
    # foo: bar
    
# must be str:str
units_data:
    # baz: qux
