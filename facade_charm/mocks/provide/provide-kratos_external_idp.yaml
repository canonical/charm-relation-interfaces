
# databag template for kratos_external_idp provider:
# this data will be put in the application/unit databag of any relation bound on provide-kratos_external_idp

# must be str:str
app_data: 
    # foo: bar
    
# must be str:str
units_data:
    # baz: qux
