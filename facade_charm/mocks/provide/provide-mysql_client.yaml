
# databag template for mysql_client provider:
# this data will be put in the application/unit databag of any relation bound on provide-mysql_client

# must be str:str
app_data: 
    # foo: bar
    
# must be str:str
units_data:
    # baz: qux
