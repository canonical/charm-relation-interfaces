
# databag template for ceph-bootstrap requirer:
# this data will be put in the application/unit databag of any relation bound on require-ceph-bootstrap

# must be str:str
app_data: 
    # foo: bar
    
# must be str:str
units_data:
    # baz: qux
