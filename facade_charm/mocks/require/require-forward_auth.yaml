
# databag template for forward_auth requirer:
# this data will be put in the application/unit databag of any relation bound on require-forward_auth

# must be str:str
app_data: 
    # foo: bar
    
# must be str:str
units_data:
    # baz: qux
