
# databag template for external_cloud_provider requirer:
# this data will be put in the application/unit databag of any relation bound on require-external_cloud_provider

# must be str:str
app_data: 
    # foo: bar
    
# must be str:str
units_data:
    # baz: qux
