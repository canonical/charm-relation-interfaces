
# databag template for nrpe-external-master requirer:
# this data will be put in the application/unit databag of any relation bound on require-nrpe-external-master

# must be str:str
app_data: 
    # foo: bar
    
# must be str:str
units_data:
    # baz: qux
